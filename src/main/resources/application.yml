server.port: 8083
spring:
    application.name: hcbc
    jpa.database: mysql
    datasource:
        url: "jdbc:mysql://mysql:3306/hcbc"
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
server:
    servlet:
        context-path: /api

